mealCost = input("");
tipPercent = input("");
taxPercent = input("");

tip = mealCost * (tipPercent / 100);
tax = mealCost * (taxPercent / 100);
totalCost = mealCost + tip + tax;

roundedTotalCost = round(totalCost);

disp(cstrcat("The total meal cost is ", int2str(roundedTotalCost), " dollars."));
